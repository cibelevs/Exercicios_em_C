//Faça um programa em C que leia uma sequência de números inteiros(a sequência 
//termina quando o usuário digitar um número negativo) e exiba o maior número digitado.


#include <stdio.h>
int main() 
{
    int n, prox;
    int termo1 = 0;
    int termo2 = 1;
    
    printf("Digite o numero do termo desejado sequência de Fibonacci: ");
    scanf("%d", &n);

   for (int i = 2; i <= n; i++)
   {
        prox = termo1 + termo2;
        termo1 = termo2;
        termo2 = prox;
    }

    printf("O %dº termo da sequência de Fibonacci é: %d\n", n, prox);
    
    return 0;
}
